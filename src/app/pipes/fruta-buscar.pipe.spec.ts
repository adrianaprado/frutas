import { FrutaBuscarPipe } from './fruta-buscar.pipe';

describe('FrutaBuscarPipe', () => {
  it('create an instance', () => {
    const pipe = new FrutaBuscarPipe();
    expect(pipe).toBeTruthy();
  });
});
